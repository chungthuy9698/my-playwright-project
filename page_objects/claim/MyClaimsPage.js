import { BasePage } from '../BasePage';
import { NotificationHelper } from '../../helper/NotificationHelper';

export class MyClaimsPage extends BasePage {
    constructor(page) {
        super(page);
    }
}